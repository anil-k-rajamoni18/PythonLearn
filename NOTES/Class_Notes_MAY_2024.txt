FUNDAMENTALS 
======================
- Computer Architecture
	Software 
	Hardware 

		Users
	-------------------
		Applications
	----------------
		OS 
	---------------
		Kernel (core part of os)
	----------------
		Hardware
		
 2 ways to interact with OS 
 ================================
 GUI : Graphical user interface ( interacing with screen , navigation)
 CLI : Command Line Interface (interacting with commands)
 
 Windows - Command Prompt, Powershell 
 Mac-os - Terminal
 Linux - Shell , bash 
 
 COMMANDS 
 ------------------
 WINDOWS 
	 dir 							- display the current path 
	 cd dirName/foldername/path 	- to change the path 
	 mkdir dirName 					- to create new dir or folder
	 rmdir dirName 					- to remove the dir or folder
	 cd .. 							- backward path change 
	 notepad filename.extension 	- creates new file 
	 type filename 					- display the content of the file
	 
 MAC-OS 
 ------------
	ls , ls -a , ls -ltr 			- display the list contents
	pwd								- display current curren path 
	cd 	dirName						- change the path
	mkdir dirName 					- create a new floder/dir
	rmdir dirName 					- delete existing empty dir/floder
	rm -R dir/floder 				- delete existing non empty dir/floder
	touch filename.ext 				- creates new file.
	cat filename					- display the content of the file.
	open filename 					- opens the file 
	
	python3 --version 
	python3 -V
	python --version 
	python -V
	
	
	
	
	
 
TO CHECK LOCAL INTERPRETER 
-----------------------------
	WIN_OS 
	-----------------
	python --version
	python -V

	MAC OS / LINUX 
	-------------------
	python3 --version
	python3 --version
	
HOW TO RUN PYTHON FILES 
=======================================
	python filename 
	
	
ASSIGNMENT
===============
- Compiler vs interperter? 
- What is bytecode? (intermediate code generated by compilers/interpreter during compilation)
	- it is machine indepedent code 
	- Write Once Run Any Where (WORA)
	
- What is ASCII & UNICODE, how string/alphabets stores in programming lang?
- what is git & github?


##########################
print() - display 
input() - reading input, by default it will read everything as string
type() - display the data type of a variable.
id() - display memory address of a variable
len() - display size of collectional data type/ no of elements 
dir() - display the directory content of a variable/object [ attributes , methods]
help() - display the documentation of a class 
ord(char) - display the ascii mapped decimal no
chr(num) - display the mapped character

TYPE CASTING 
-------------------
	int(), float(), bool(), complex(), str()
	list(), tuple(), set(), dict()
	

DATA TYPES 
---------------
	primitive data types : interger, float, string, complex, boolean, None 
	collectional data types: List, Tuple, Set, Dictionary
	